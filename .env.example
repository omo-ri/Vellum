# 复制为 .env 后使用：cp .env.example .env（make setup 会代劳）
#
# 这些变量由 Makefile include 并 export，二进制本身不认识 .env 文件。
# 每一个都是必填的——没有任何默认值，缺一个进程就拒绝启动。理由见 docs/architecture.md。

# dev → slog 用 TextHandler；prod → JSONHandler
APP_ENV=dev

# Echo 的监听地址，冒号开头表示监听所有网卡
HTTP_ADDR=:8080

# 「今天」「本月」的唯一裁决依据。数据库里一律存 UTC。
SITE_TIMEZONE=Europe/Moscow

# Postgres 连接。DSN 由 internal/platform/config 拼装，是全站唯一的拼装处。
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vellum
DB_USER=vellum
DB_PASSWORD=vellum
