# PRD：Vellum

这份文档回答的是**「要做什么、为谁做、明确不做什么」**。

读它不需要知道这个项目是用什么语言写的。实现看 [`docs/`](docs/)，一个概念叫什么以及它在代码里写成什么看 [`UBIQUITOUS_LANGUAGE.md`](UBIQUITOUS_LANGUAGE.md)——**本文档与它冲突时以它为准**。

## 站主是谁

一名在俄罗斯的中国留学生。本站只有他一个用户，没有第二个人会登录，也不打算有。

他同时是这个项目的开发者，并且希望借它扎实地学会两件事：**正确使用 Postgres**，以及**一套完整的 CI/CD 工作流**。这两个学习目标是刻意保留的非最优选择，但它们必须服务于「这个站我每天都会用」这个第一目标，而不是相反。

## 三件反复发生、但目前没有落点的事

1. **生活费心算。** 每月有一笔固定的生活费（卢布），需要随时知道「这个月还剩多少能花」「今天大概能花多少」。目前靠脑内估算和银行 App 的流水回溯，既不准也不及时；房租水电这类月初就已知的大额固定支出会让心算彻底失真。
2. **菜谱散落。** 喜欢的菜谱分散在聊天记录、网页截图、视频收藏里，做饭前找不到，做完也没有地方记「这道菜做过几次、大概花多少钱、上次是什么时候做的」。
3. **重要照片被淹没。** 手机相册里几万张照片，真正想反复回看的只有几百张。它们没有被组织成「历程」，只是按时间堆着。

市面上每一件事都有成熟产品，但它们都是别人的：数据在别人的服务器上，界面是大众审美，功能是为所有人设计的。站主想要的不是三个工具，而是**一个属于自己的地方**——像家，或者像一本手账。它的主题、语气、外观、功能，全部按自己的喜好来。

## 解法

**Vellum** —— 一个单用户、自托管、公网可达的个人网站，主题为「日式异世界旅人的据点」。

站主登录后进入**据点主页**，页面同时是导航和今日状态面板：今天还能花多少钱、最近的一段回忆、一道可以做的菜。从这里进入三个彼此独立的模块：

- **理财** —— 设定月生活费预算，记录每一笔开销，实时得到本月剩余和今天可用。用不完的钱月末自动进入存款，大额开销从存款支出而不污染月度预算。
- **菜谱** —— 每道菜有名字、成品照片、结构化食材列表、自由书写的步骤、做过次数、上次做于、成本估算和标签。
- **相册** —— 以「事件」为一级单位的编年史：一次旅行、一个学期、一件记得住的事。事件有标题、日期区间和一段文字，照片附属其下，每张可以写一句话。

它是一个可以添加到手机主屏的网站，在餐厅和超市随手记账。

## 最高优先级

**第一目标始终是「这个站我每天都会用」。** 当工程优雅与使用频率冲突时，使用频率优先。

**记账流程的速度（金额 → 分类 → 完成）是理财模块的生死线**，任何增加记账摩擦的设计都应当被拒绝。

## 需求

### 认证与访问

1. 作为站主，我想用用户名和密码登录，以便只有我能访问站内的全部内容。
2. 作为站主，我想在登录后保持登录状态约 30 天，以便日常使用时不必反复输入密码。
3. 作为站主，我想主动登出，以便在借用他人设备后终止会话。
4. 作为站主，我想让所有未登录的请求都被拒绝并跳转到登录页，以便我的开销记录和私人照片不会被公网上的陌生人看到。
5. 作为站主，我想让连续多次失败的登录尝试被暂时锁定，以便公网上的自动化爆破无法穷举我的密码。
6. 作为站主，我想修改自己的密码，以便在怀疑泄露时可以自救。
7. 作为站主，我想把这个站添加到手机主屏并以全屏方式打开，以便它用起来像一个 App 而不是一个书签。
8. 作为站主，我想在手机上流畅地使用所有功能，以便我能在餐厅、超市、路上随手记录。

### 据点主页

9. 作为站主，我想在主页看到通往三个模块的入口，以便一眼就知道能做什么。
10. 作为站主，我想在主页看到「今天还能花多少钱」，以便在决定要不要买某样东西时立刻有依据。
11. 作为站主，我想在主页看到本月剩余预算和存款余额，以便掌握整体财务状态。
12. 作为站主，我想在主页看到最近一段回忆的封面和标题，以便每次打开都有回到家的感觉。
13. 作为站主，我想在主页看到一道随机或最近的菜谱，以便在不知道吃什么的时候得到提示。
14. 作为站主，我想让主页在任何一个模块还没有数据时也能正常显示，以便新装的站不会一片报错。

### 理财 —— 预算

15. 作为站主，我想为某个月设置生活费预算总额，以便所有剩余额的计算有基准。
16. 作为站主，我想在月中修改当月预算，以便家里追加汇款或计划变化时数字仍然是真的。
17. 作为站主，我想让新的一个月自动沿用上个月的预算金额作为默认值，以便不用每月重复设置。
18. 作为站主，我想看到本月剩余，以便知道整月还剩多少。
19. 作为站主，我想看到今天可用，以便得到一个可以直接指导今天行为的数字。
20. 作为站主，我想让今天超支后明天的今天可用自动变少、今天省下后明天自动变多，以便这个数字始终自我纠偏。
21. 作为站主，我想在本月剩余为负时看到明确的超支提示，以便我不会误以为还有钱。

### 理财 —— 记账

22. 作为站主，我想用「金额 + 分类 + 备注」记录一笔开销，以便记账过程足够快、我不会因为嫌麻烦而放弃。
23. 作为站主，我想让记账默认使用今天的日期，以便绝大多数情况下我不需要碰日期选择器。
24. 作为站主，我想在需要时把一笔开销的日期改成过去的某一天，以便补记忘记记的账。
25. 作为站主，我想把一笔开销标记为固定开销，以便房租、水电、话费这类支出可以在月初就先记进去。
26. 作为站主，我想在账单实际到达后直接编辑那条固定开销的金额，以便预估值变成实际值，而不需要额外的「冲抵」概念。
27. 作为站主，我想把一笔开销标记为大额开销，以便机票、电脑、学费这类支出不会把当月的今天可用算崩。
28. 作为站主，我想让大额开销从存款余额里扣除，以便「日常省钱 → 攒下 → 支付大件」这条线是闭合的。
29. 作为站主，我想编辑任何一笔已记录的开销，以便修正记错的金额或分类。
30. 作为站主，我想删除一笔记错的开销，以便账面保持真实。
31. 作为站主，我想按月份浏览所有开销记录，以便回顾这个月的钱都花在哪了。
32. 作为站主，我想在浏览时按分类筛选开销，以便看清某一类支出的规模。

### 理财 —— 分类与存款

33. 作为站主，我想在初次使用时就有一套可用的预设分类，以便不必先做配置才能记第一笔账。
34. 作为站主，我想新增自己的分类，以便账目贴合我真实的生活结构。
35. 作为站主，我想重命名分类，以便叫法可以随时调整。
36. 作为站主，我想归档不再使用的分类，以便它从记账下拉框里消失，但历史账目仍然显示正确的分类名。
37. 作为站主，我想看到当前的存款余额，以便知道自己实际攒下了多少。
38. 作为站主，我想让每个月结束时的本月剩余自动进入存款，以便省下来的钱有一个明确的去处。
39. 作为站主，我想让超支的月份从存款里扣掉相应金额，以便存款始终是一个诚实的数字。
40. 作为站主，我想查看存款流水，以便知道每一笔增减是从哪来的。
41. 作为站主，我想看到每个已结束月份的结算记录（预算、实际支出、结余），以便回顾自己的历史表现。

### 菜谱

42. 作为站主，我想新增一道菜谱，包含名字、成品照片、食材、步骤、成本估算和标签，以便把喜欢的菜从聊天记录里收拢到一个地方。
43. 作为站主，我想用「名称 + 数量 + 单位」的结构化列表记录食材，以便做饭前能清楚地扫一眼要买什么。
44. 作为站主，我想调整食材的顺序，以便列表符合我实际采买的习惯。
45. 作为站主，我想用自由的富文本（Markdown）书写步骤，以便写菜谱时不被表单结构限制。
46. 作为站主，我想为每道菜上传一张成品照片，以便菜谱列表看起来像一本料理书而不是一张表格。
47. 作为站主，我想记录这道菜的成本估算，以便在预算紧张时知道做哪道菜更划算。
48. 作为站主，我想给菜谱打多个标签，以便按「快手」「汤」「中餐」这类维度归类。
49. 作为站主，我想按标签筛选菜谱，以便快速缩小选择范围。
50. 作为站主，我想按名字搜索菜谱，以便直接找到想做的那道。
51. 作为站主，我想在做完一道菜后点一下「今天做了」，以便系统自动把做过次数加一并更新上次做于。
52. 作为站主，我想看到每道菜做过几次、上次做于什么时候，以便知道自己最近吃得是不是太重复了。
53. 作为站主，我想编辑和删除菜谱，以便内容可以持续修正。

### 相册

54. 作为站主，我想以「事件」为单位组织照片，以便这个模块是一段历程而不是一堆文件。
55. 作为站主，我想给事件设置标题、日期区间和一段文字，以便照片有语境、几年后我还知道那是什么。
56. 作为站主，我想给事件指定一张封面照片，以便时间轴上每段回忆都有面孔。
57. 作为站主，我想在一个竖向时间轴上按时间倒序浏览所有事件，以便回看时像翻一本手账。
58. 作为站主，我想一次上传多张照片到某个事件，以便整理一次旅行不必重复操作几十遍。
59. 作为站主，我想在手机浏览器里直接从相册选照片上传，以便不需要先把照片导到电脑。
60. 作为站主，我想让上传后的照片自动生成缩略图和预览图，以便在手机流量下浏览时不必加载几 MB 的原图。
61. 作为站主，我想让照片保留原图，以便这个站是我这些照片的可靠归宿。
62. 作为站主，我想给每张照片写一句话说明，以便照片墙有温度而不只是像素。
63. 作为站主，我想让照片按拍摄时间自动排序，以便一次旅行的照片顺序天然正确。
64. 作为站主，我想手动调整某些照片的顺序，以便叙事可以按我的意愿编排。
65. 作为站主，我想点开一张照片查看大图并左右切换，以便沉浸地回看。
66. 作为站主，我想删除照片或整个事件，以便内容可以整理。

### 主题与观感

67. 作为站主，我想让整站呈现「异世界旅人的据点」的视觉基调（羊皮纸底色、明朝体/手写体标题、暗金与墨绿），以便打开它的时候我是高兴的。
68. 作为站主，我想让标题使用日文、正文使用中文，以便氛围拉满而不需要一套 i18n 机制。
69. 作为站主，我想让所有颜色、间距、字号来自同一份设计令牌，以便整站风格不会随着功能增加而漂移。
70. 作为站主，我想有一个只有我能访问的组件预览页，以便调整视觉基调时能一次看到所有组件。

> 面向开发者的那部分要求（CI/CD、模块边界、分层、测试）不在这里——它们不是产品需求，落点是 [`docs/architecture.md`](docs/architecture.md)、[`docs/testing.md`](docs/testing.md) 与 [`docs/operations.md`](docs/operations.md)。

## 业务规则

术语的精确定义在 [`UBIQUITOUS_LANGUAGE.md`](UBIQUITOUS_LANGUAGE.md)，这里只给算法。

- **本月已花** = 当月弹性开销与固定开销之和。
- **本月剩余** = 当月预算 − 本月已花。**可以为负**，为负时前端显式提示超支。
- **今天可用** = 本月剩余 ÷ 本月剩余天数（含今天），向下取整。这是滚动式的：今天多花，明天自动变少；今天省下，明天自动变多。由于固定开销在月初就已记入，这个数字从每月 1 号起就是可信的。
- **固定开销**在计算上与弹性开销完全一样，都吃预算。它唯一的特别之处是站主在月初就提前记了。它**不是**周期性模板机制——账单到达后直接编辑那笔开销的金额，没有「冲抵」概念。
- **大额开销**不参与上述任何计算，记录时同步产生一条等额的负向存款流水。
- **月末结算**：一个月结束后，把该月的本月剩余（正或负）写入存款流水，并生成一条不可变的结算记录。超支的月份照样写，从存款里扣——存款余额必须是一个诚实的数字，不能只记好消息。
- **存款余额**永远等于全部存款流水之和，没有第二个来源。
- 「今天」与「本月」一律按**站点时区**划分，不是 UTC 日，也不是浏览器时区日。本站只有一个用户，也只有一个时区。

## 明确不做

以下内容经过讨论后被**否决**，不应实现，也不应为其生成任务。

| 不做 | 因为 |
| --- | --- |
| 备份、恢复与数据冗余 | 本期不做 |
| 离线记账与离线同步（含 CRDT、冲突解决） | 添加到主屏是为了打开快，不是为了离线可用 |
| 账单 OCR、小票识别、银行对接、CSV 导入 | 记账一律手动。识别的准确率不够，校对比手输还慢 |
| 多币种与汇率 | 生活在一个国家，只花一种钱 |
| 跨月预算结转 | 剩余进入存款，下个月预算重新开始——这才是「省下来的钱有去处」 |
| 份量换算（2 人份 → 4 人份） | 食材虽为结构化，本期不实现换算 |
| 存钱目标 / 讨伐目标 | 存款本期只有余额与流水 |
| 等级、经验值、成就、连续打卡等游戏化机制 | 这个站是据点，不是任务面板 |
| 跨模块聚合时间轴 | 据点主页的聚合发生在前端，后端不做跨模块查询 |
| 多用户、注册、找回密码、权限系统 | 全站单用户 |

工程侧刻意不引入的东西（DI 框架、消息队列、Kubernetes 等）见 [`docs/architecture.md`](docs/architecture.md) 的「不做什么」。

## 里程碑

| | 内容 | 为什么排在这里 |
| --- | --- | --- |
| **M0** | 没有任何业务功能，但已经能 `git push` 自动上线：认证与登录页、测试脚手架、CI/CD 全链路真实部署 | 一个只返回健康状态、但真的在公网上跑着的服务，比一个功能完整却从未部署过的本地项目更有价值 |
| **M1** | 理财模块完整功能 | 视觉是这个项目最好玩的部分，也因此最容易吞掉全部时间。先让一个朴素但每天都会用的记账功能上线，项目才不会烂尾 |
| **M2** | 设计令牌与视觉基调落地、据点主页 | 有了每天都用的功能之后，再让它好看 |
| **M3** | 菜谱模块 | |
| **M4** | 相册模块 | 图片处理是三个模块里技术面最宽的，放最后 |

M0 的具体待办清单在 [`README.md`](README.md)——那里回答「现在做到哪」，这里只回答「为什么是这个顺序」。

## 两条产品约束

- **命名分两层。** 本文档与领域语言文档都使用中性命名（「理财」「相册」「据点主页」「存款」）。世界观与日文命名表（例如《収支の記録》《料理魔導書》《記憶の結晶》，存款可显示为「貯金」）是叠在其上的**展示层**，只替换用户可见文案，不改动概念划分与代码标识符。实现时把用户可见文案集中放置，以降低这次替换的成本。
- **不使用任何受版权保护的素材。** 主题灵感来自日式异世界作品，但站内不得使用任何原作的插画、角色、logo 或字体资产。视觉效果一律由 CSS 与自由许可字体实现。
