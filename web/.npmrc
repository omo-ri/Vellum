# store 必须与 node_modules 同处一个文件系统。
#
# pnpm 靠硬链接把 store 里的文件接进 node_modules，而硬链接跨不了文件系统边界。
# 代码在 Windows 盘（/mnt/z，9p）上，store 若留在默认的 ~/.local/share/pnpm/store
# （WSL 的 ext4）就会跨盘，pnpm 只能退化成整目录复制——那是这个项目里最慢的操作。
store-dir=/mnt/z/.pnpm-store
