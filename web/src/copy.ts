// 全站用户可见文案的唯一出处。组件里不写字面量文案。
//
// 命名分两层：这里是中性命名层。将来的日文术语书是叠在其上的展示层，它替换的正是
// 本文件里的字符串，不改动组件结构与代码标识符（见 PRD.md 的「命名分两层」）。
// 文案散在各组件里的话，那次替换就要翻遍整个 web/src。
export const copy = {
  connection: {
    checking: "正在确认据点状态……",
    ok: "据点连接正常",
    down: "连接不上据点",
  },
} as const;
