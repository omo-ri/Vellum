# Ubiquitous Language

Vellum 的领域语言。**这是中性命名层**：定义概念本身，以及它在代码里的标识符。后续的「术语书」（日文个性化命名）是叠在这一层之上的**展示层**——它替换用户可见文案，不替换本文档的概念与代码标识符。

「代码标识符」列是规范性的：数据库列名、Go 类型与字段、OpenAPI schema 名一律采用该列的写法。

## 全站与平台

| 术语 | 定义 | 代码标识符 | 应避免的别名 |
| --- | --- | --- | --- |
| **Vellum** | 这个单用户、自托管的个人网站本身 | — | 系统、平台、App |
| **站主** | 本站唯一的人类使用者，同时是唯一的认证身份 | `owner` / `account` | 用户、user、管理员 |
| **会话** | 一次登录产生的、有过期时间的凭据，通过 HttpOnly cookie 传递 | `session` | token、登录态、JWT |
| **据点主页** | 登录后的落地页，同时是导航与今日状态面板 | `home` | 首页、dashboard、仪表盘 |
| **模块** | `finance` / `recipe` / `album` 三个互不引用的业务领域之一 | `module` | 功能、板块、服务 |
| **站点时区** | 配置项固定的站主所在地时区，是「今天」「本月」的唯一裁决依据 | `site_timezone` | 本地时区、当地时间 |
| **今天** | 按**站点时区**划分的自然日，不是 UTC 日、也不是浏览器时区日 | `today` | 当天、当日 |
| **本月** | 按**站点时区**划分的自然月，是预算与结算的基本周期 | `month` | 当月、月度周期 |

## 理财 —— 预算与计算

| 术语 | 定义 | 代码标识符 | 应避免的别名 |
| --- | --- | --- | --- |
| **预算** | 某个自然月可供日常花销的生活费总额（RUB 最小单位） | `budget` | 额度、生活费、限额 |
| **本月已花** | 当月**弹性开销**与**固定开销**之和 | `spent` | 已支出、消费额、总花费 |
| **本月剩余** | 预算 − 本月已花，可以为负 | `remaining` | 余额、剩余额度、结余 |
| **今天可用** | 本月剩余 ÷ 本月剩余天数（含今天），向下取整 | `daily_allowance` | 每日预算、日限额、今日额度 |
| **超支** | 本月剩余为负的状态 | `overspent` | 透支、爆预算 |

## 理财 —— 记账

| 术语 | 定义 | 代码标识符 | 应避免的别名 |
| --- | --- | --- | --- |
| **开销** | 一笔已发生的花钱记录：日期、金额、分类、备注、种类 | `expense` | 支出、消费、条目、entry、交易 |
| **种类** | 开销的三选一属性，决定它如何参与计算 | `kind` | 类型、type、性质 |
| **弹性开销** | 计入当月预算的日常开销，是默认种类 | `flexible` | 日常开销、普通开销、变动支出 |
| **固定开销** | 月初即已知、由站主手动提前记入的开销，计入当月预算 | `fixed` | 周期性开销、订阅、账单、模板 |
| **大额开销** | 不计入当月预算、改从**存款**扣除的一次性大宗支出 | `large` | 大件、特殊支出、非常规支出 |
| **分类** | 开销的归类标签，可增删改与归档 | `category` | 类目、标签、tag |
| **归档** | 把分类从新记账的可选项中移除，但历史开销仍显示其名称 | `archived` | 停用、删除、隐藏 |

## 理财 —— 存款与结算

| 术语 | 定义 | 代码标识符 | 应避免的别名 |
| --- | --- | --- | --- |
| **存款** | 日常省下的钱累积成的、用于支付大额开销的资金池 | `savings` | 貯金、储蓄、小金库、存钱罐 |
| **存款流水** | 存款的逐笔增减记录，是余额的唯一事实来源 | `savings_ledger` | 流水、账本、变动记录 |
| **存款余额** | 存款流水的求和结果，不单独存储 | `savings_balance` | 总额、貯金余额、结余 |
| **月末结算** | 一个月结束后把该月**结余**写入存款流水并生成不可变记录的动作 | `month_settlement` | 月结、对账、清算 |
| **结余** | 月末结算时被冻结下来的那个月的本月剩余，正负皆可 | `surplus` | 剩余、余额、盈余 |
| **惰性触发** | 结算由「下一次任意 API 请求」顺带补齐，而非由调度器发起 | `lazy` | 定时任务、cron、后台任务 |

## 菜谱

| 术语 | 定义 | 代码标识符 | 应避免的别名 |
| --- | --- | --- | --- |
| **菜谱** | 一道菜的完整记录：名称、封面、食材、步骤、成本估算、标签、烹饪历史 | `recipe` | 食谱、料理、菜品 |
| **食材** | 菜谱中「名称 + 数量 + 单位」的一条结构化条目 | `ingredient` | 材料、原料、配料 |
| **步骤** | 菜谱的自由书写部分（Markdown），不做结构化拆分 | `steps` | 做法、流程、说明 |
| **标签** | 菜谱的多对多分类维度（如「快手」「汤」） | `tag` | 分类、category |
| **成本估算** | 做这道菜大概要花多少钱 | `estimated_cost` | 预算、价格、花费 |
| **做过次数** | 这道菜被记录为「今天做了」的累计次数 | `cook_count` | 烹饪次数、频次 |
| **上次做于** | 最近一次「今天做了」的日期 | `last_cooked_at` | 最后制作时间、上次烹饪 |
| **今天做了** | 站主完成一道菜后的一次点击，使做过次数加一并更新上次做于 | `cook` | 打卡、完成、记录一次 |

## 相册

| 术语 | 定义 | 代码标识符 | 应避免的别名 |
| --- | --- | --- | --- |
| **事件** | 相册的一级单位：一段有标题、日期区间与正文的经历 | `event` | 回忆、相簿、集合、专辑 |
| **照片** | 附属于某个事件的一张图片及其元数据 | `photo` | 图片、影像、image |
| **原图** | 上传时的未经处理的图片文件，永久保留 | `original` | 源文件、大图 |
| **预览图** | 约 1600px 的派生图，用于点开查看 | `preview` | 中图、大图 |
| **缩略图** | 约 400px 的派生图，用于列表与网格 | `thumbnail` | 小图、封面图 |
| **封面** | 代表一个事件（或一道菜谱）的那张照片 | `cover` | 主图、题图 |
| **说明** | 写在单张照片上的一句话 | `caption` | 描述、备注、注释 |
| **拍摄时间** | 从 EXIF 提取的时间，是照片的默认排序依据 | `taken_at` | 创建时间、日期 |
| **时间轴** | 事件按时间倒序排列的竖向浏览视图 | `timeline` | 编年史、列表、feed |

## Relationships

- 一个**站主**拥有零到多个**会话**；一个**会话**属于唯一的**站主**。
- 一个**月**有零或一条**预算**；**本月剩余**由该月预算与该月全部**弹性开销**、**固定开销**导出，不存储。
- 一笔**开销**属于恰好一个**分类**和恰好一种**种类**。
- 一笔**大额开销**产生恰好一条负向**存款流水**；**弹性开销**与**固定开销**不产生任何存款流水。
- 一次**月末结算**产生恰好一条**存款流水**与一条不可变的结算记录；同一个月只能结算一次。
- **存款余额**永远等于全部**存款流水**之和——它没有第二个来源。
- 一道**菜谱**有零到多个**食材**、零到多个**标签**、零或一张**封面**。
- 一个**事件**有零到多张**照片**；**封面**必须是该事件自己的**照片**之一。
- 每张**照片**恰好有一份**原图**、一张**预览图**、一张**缩略图**。
- **菜谱**与**照片**都通过 `platform` 存图，但它们的元数据互不可见——**模块**之间不存在引用。

## Example dialogue

> **Dev：** 月初把房租记成**固定开销**，那它算进**本月已花**吗？

> **站主：** 算。**固定开销**和**弹性开销**在计算上完全一样，都吃**预算**。它唯一的特别之处是我在月初就提前记了——这样从 1 号起**今天可用**就是可信的，不会到月中被房租打崩。

> **Dev：** 那账单实际到了、金额和预估不一样呢？要不要再记一笔冲抵？

> **站主：** 不要，直接编辑那笔**开销**的金额。**固定开销**不是周期性模板，它就是一笔提前记的账。

> **Dev：** 明白。买笔记本电脑这种呢？

> **站主：** 那是**大额开销**。它完全不进**本月已花**，也不影响**今天可用**，而是生成一条负向**存款流水**从**存款余额**里扣。日常省下来的钱通过**月末结算**流进**存款**，大件从**存款**出——这条线是闭合的。

> **Dev：** 如果那个月**超支**了呢，**结余**是负数，也照样进**存款流水**？

> **站主：** 照样进，扣掉。**存款余额**必须是一个诚实的数字，不能只记好消息。

## Flagged ambiguities

1. ~~**「预算」被用于两个无关的概念。**~~ **已解决**：菜谱的「大概预算」改称**成本估算**（`estimated_cost`），spec 已同步；「预算」一词在全站只保留理财含义。
2. **「今天可用」与「每日预算」混用。** spec 中「不会把当月的每日预算算崩」指的就是**今天可用**。canonical 取**今天可用**——「每日预算」暗示一个被预先设定的固定值，而这个数字实际上是每天重算的滚动量。
3. ~~**「开销」「支出」「条目 / entry」三词混用。**~~ **已解决**：领域语言统一为**开销**，代码标识符统一为 `expense`；spec 中 `finance` schema 的表名已由 `entry` 改为 `expense`。
4. **「本月剩余」与「结余」是同一个量的两个生命阶段，不可混用。** **本月剩余**是当月实时导出的活数字，会随每笔记账变化；**结余**是**月末结算**时把它冻结下来的历史值，写入后不可变。谈进行中的月份只能说「本月剩余」，谈已结束的月份只能说「结余」。
5. **「回忆」不是模型名。** spec 中「最近一段回忆」指的是**事件**。「回忆」可以作为界面文案（并且很可能被术语书替换成日文名），但在代码、API 与技术讨论中一律用**事件**。
6. **「分类」与「标签」不是同一种东西。** 理财的**分类**是单选、可归档、有排序；菜谱的**标签**是多对多、无归档概念。不要因为两者都是「归类」就共用词或抽象成同一套机制——它们分属两个不相往来的**模块**。
7. **「今天」在跨时区场景下是有歧义的**，本项目消除歧义的方式是：一切「今天」「本月」按**站点时区**计算，数据库一律存 UTC。任何地方出现「按用户本地时间」的表述都是错的——本站只有一个用户，也只有一个时区。
8. **「貯金」已全站改为「存款」。** 原 spec 混用了日文的「貯金」与中文的「存款/储蓄」，现统一为中性词**存款**（`savings`）。日文的「貯金」交给术语书作为**展示文案**使用——展示层可以叫它貯金，但代码标识符、API 字段与技术讨论一律用**存款 / `savings`**。这条同时确立了通则：术语书只叠加显示名，不改动本文档的概念划分与代码标识符。
9. **「分类」上表写的是「可增删改与归档」，但需求里没有删除。** [`PRD.md`](PRD.md) 的需求 33–36 只有新增、重命名、归档；而 `expense` 有外键指向 `category`，删掉一个被引用的分类，历史账目就崩了——这正是「归档」存在的理由。待定，见 [`docs/open-questions.md`](docs/open-questions.md) 的 OQ-13，定案后改上表那一行。
10. **「本月剩余」与「结余」的分界，在已结算月份被事后改动时会失效。** 见 OQ-5——如果允许修改已结算月份的开销，那个被冻结下来的「结余」就不再是那个月的真相。第 4 条那道干净的分界，要等 OQ-5 定案才真正成立。
